[user]
    name = shendypratamaa
    email = shendypratamaa@gmail.com

[http]
    postBuffer = 157286400
    sslVerify = true
    sslCAInfo = /etc/ssl/cert.pem

[credential]
    helper = cache

[pull]
    rebase = false

[gpg]
    program = /opt/homebrew/bin/gpg

[core]
    pager = delta
    editor = nvim

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    light = false
    side-by-side = false
    line-numbers = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[pretty]
    dateline = format:%C(yellow bold)%h%d%Creset %C(green bold)(%cr)%Creset %C(red)%cn%Creset %n %x09 %C(bold)%s%Creset

[format]
    pretty = dateline
